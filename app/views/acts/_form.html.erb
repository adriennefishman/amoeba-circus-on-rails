<%= form_for(@act) do |f| %>
  <% if @act.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@act.errors.count, "error") %> prohibited this act from being saved:</h2>

      <ul>
      <% @act.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :date %><br>
    <%= f.date_field :date %>
  </p>
  <p>
    <%= f.label :time %><br>
    <%= f.time_select :time %>
  </p>
<!--   <p>
    <%= f.label :amoebas, "Select Amoebas" %>
    <%= f.collection_check_boxes(:amoebas, Amoeba.all, :id, :name)%>
  </p> -->
<!--   <p>
    <%= f.fields_for :amoebas, Amoeba.new do |amoeba_form| %>
    <% Amoeba.all.each do |amoeba| %>
    <%= amoeba_form.check_box :name, {}, amoeba.name %>
    <%= amoeba.name %>
    <% end %>
    <%end %>
  </p> -->
<!--   <p>
    <% Amoeba.all.each do |amoeba| %>
    <input type="checkbox" name="act[amoebas_attributes][][name]" value="<%= amoeba.name %>"><%= amoeba.name %>
    <% end %>
  </p> -->

    <%= f.submit %>
  </div>
<% end %>
